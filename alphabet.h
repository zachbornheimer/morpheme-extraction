/*
 * alphabet.h
 * Defines the prototypes for alphabet.c
 */

#ifndef ALPHABET_PROTO
#define ALPHABET_PROTO

/* for alphabet extraction */
char* __gf(char[], int*, int*);
char* getfiles(int*);
char* find_word_delimiter(char**);

#endif
